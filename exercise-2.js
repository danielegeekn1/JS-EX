/*const concat = function (arr1, arr2) {
  return arr1.concat(arr2);
}

console.log(concat([2, 4], [5, 1]));*/

//prima variabile funzione, scritta come un'arrow function
const concat = (arr1, arr2) => {
  return arr1.concat(arr2);
}
  
console.log (concat([2, 4], [5, 1]));